spring:
  datasource:
    url: jdbc:oracle:${DB_HOMOLOG_SERVER:thin:@oracle.fiap.com.br}:${DB_HOMOLOG_PORT:1521}:${DB_HOMOLOG_NAME:ORCL}
    username: ${DB_HOMOLOG_USERNAME:RM550415}
    password: ${DB_HOMOLOG_PASSWORD:080704}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: true
